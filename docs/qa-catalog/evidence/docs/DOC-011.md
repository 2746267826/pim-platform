# DOC-011 | docs/superpowers/specs/2026-08-10-pim-docker-deployment-design.md | 一般 | Docker 部署设计稿仍描述 SSH 通道与已移除实现不一致
- 描述：Docker 部署设计稿以 SSH 为核心运维通道，但该通道已在 ops-readonly-api 迭代中物理移除。
- 文档位置：specs/2026-08-10-pim-docker-deployment-design.md:16-18 容器 SSH / 密钥注入表格 与 59-117 entrypoint/compose 片段
- 文档承诺：`容器 SSH | sshd 监听 22；宿主机映射 127.0.0.1:${PIM_SSH_PORT:-2222}:22；仅密钥认证` 与 `PIM_SSH_AUTHORIZED_KEYS base64 单行`
- 代码实际：`src/Pim.Api/Dockerfile` 无 `openssh-server`，仅 `supervisor/tini`；`scripts/docker/entrypoint.sh` 无 `base64 -d` 写入 `authorized_keys`；`docker-compose.prod.yml` 无 `2222:22` 与 `pim_ssh_keys` 卷；`docs/ops-readonly-api.md:310` 列为已移除项
- 差异：设计稿描述的部署形态与当前代码的实际容器形态不一致，按设计稿部署会得到无效配置
