<Window x:Class="Pim.Client.App.LoginWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="PIM 登录" Height="300" Width="380"
        WindowStartupLocation="CenterScreen" ResizeMode="NoResize"
        WindowStyle="ToolWindow" ShowInTaskbar="True" Topmost="True">
    <Grid Margin="24">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <TextBlock Text="登录 PIM 以启用数据上传" FontSize="14" FontWeight="Bold" Margin="0,0,0,16"/>

        <TextBlock Grid.Row="1" Text="用户名" Margin="0,0,0,4"/>
        <TextBox x:Name="UsernameBox" Grid.Row="2" Margin="0,0,0,12" Height="28"/>

        <TextBlock Grid.Row="3" Text="密码" Margin="0,0,0,4"/>
        <PasswordBox x:Name="PasswordBox" Grid.Row="4" Margin="0,0,0,16" Height="28"/>

        <StackPanel Grid.Row="5" Orientation="Horizontal" HorizontalAlignment="Right">
            <TextBlock x:Name="ErrorText" Foreground="Red" VerticalAlignment="Center" Margin="0,0,12,0" Visibility="Collapsed"/>
            <Button x:Name="SkipButton" Content="跳过" Padding="16,6" Margin="0,0,8,0" Click="OnSkip"/>
            <Button x:Name="LoginButton" Content="登录" Padding="16,6" IsDefault="True" Click="OnLogin"/>
        </StackPanel>
    </Grid>
</Window>
