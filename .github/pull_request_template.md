## 概述 / Overview

<!-- 用一两句话说明本 PR 做什么，解决什么问题（中英双语） -->
<!-- 1-2 sentences on what this PR does and why (bilingual) -->

---

## 技术修改 / Technical changes

<!-- 改了哪些模块/文件、关键设计决策、API 或数据结构变化、依赖变更等 -->
<!-- Modules/files touched, key design decisions, API or schema changes, dependency changes -->

-

## 功能变化 / Feature changes

<!-- 用户可见的变化：新增/修改/删除的功能与行为（若无，填“无 / None”） -->
<!-- User-visible changes: features added, modified or removed (write "无 / None" if none) -->

-

## 如何体验 / How to try it

<!-- 用户视角：用户如何体验这个功能——在哪个页面/界面、做什么操作、会看到什么效果（可附操作步骤）。
     注意：这是功能体验指引，不是部署/构建说明；构建、运行、验证命令请写在「测试 / Tests」章节。 -->
<!-- User perspective: how a user experiences this feature — which page/screen, what actions to take,
     what visible effect to expect (step-by-step is fine).
     Note: this is an experience guide for users, NOT deployment/build instructions; build/run/verify
     commands belong in the 测试 / Tests section. -->

-

## 测试 / Tests

<!-- 已运行的验证命令与结果（如 dotnet test、npm run build、gradlew 等） -->
<!-- Verification commands run and their results -->

-

<!--
说明 / Note：
以上「技术修改 / 功能变化 / 如何体验」章节会被 CI 自动提取并写入 GitHub Release 更新日志，
请务必如实填写，标题格式请保持与模板一致（支持中英文标题）。
The sections above are automatically extracted by CI into the GitHub Release changelog.
Keep the heading format as-is (both Chinese and English headings are recognized).
-->
