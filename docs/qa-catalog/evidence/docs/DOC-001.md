# DOC-001 | README.md | 严重 | 生产部署单容器仍描述 SSH 通道与代码已移除不一致
- 描述：README 生产部署章节仍描述容器内 SSH 22 端口与密钥注入流程，但代码与 ops 文档已于 2026-08-21 完全移除 SSH 相关实现。
- 文档位置：README.md:178-179 单容器形态 和 210-212 密钥生成步骤
- 文档承诺：`单容器形态：HTTP（容器内 5000）+ SSH（容器内 22，用于远程管理）。` 以及 `printf 'ssh-ed25519 AAAA...\n' | base64 -w0` 与 `PIM_SSH_AUTHORIZED_KEYS` 变量
- 代码实际：`docker-compose.prod.yml` 仅暴露 `0.0.0.0:${PIM_HTTP_PORT:-5858}:5000`，无 `2222:22`；`src/Pim.Api/Dockerfile` 无 `openssh-server`、`EXPOSE 22`、`pimlog` 用户；`docs/ops-readonly-api.md:310-331` 明确列出移除项并声明原 `ssh -p 2222 pimlog@host` 已不可用
- 差异：文档承诺的 SSH 通道在代码中已物理删除，用户按 README 部署会配置无效的 `PIM_SSH_AUTHORIZED_KEYS` 与端口，无法实现文档所述的远程管理
