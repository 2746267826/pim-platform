# DOC-006 | AGENTS.md | 严重 | CORS 白名单要求与全局 AllowAnyOrigin 实现不一致
- 描述：AGENTS B5 要求外部 URL 采用白名单校验，但 API 对 CORS 允许任意源。
- 文档位置：AGENTS.md:94 B5 `Whitelist-style validation must be re-checked...` 结合 CORS 安全预期
- 文档承诺：涉及外部来源的校验应为白名单式，而非放通任意源
- 代码实际：`src/Pim.Api/Program.cs:63-66` `policy.AllowAnyOrigin().AllowAnyMethod().AllowAnyHeader()` 且 `app.UseCors()` 全局生效；`evidence/cors-allowany.txt` 已留存该片段
- 差异：文档要求白名单，代码放行 `*`，任意 Origin 可携带 `Authorization` 发起跨域请求，不符合文档安全模型
