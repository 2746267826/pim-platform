# DOC-003 | README.md | 严重 | 定位精度20m质量门与服务端阈值50m不一致
- 描述：README 对外宣称定位 20m 精度质量门，但服务端所有链路统一以 50m 为阈值。
- 文档位置：README.md:47 定位采集表格 和 141 设计逻辑 三原则
- 文档承诺：`20m 精度质量门，等 GPS 收敛才收点；信号差时收最优 fix 并明确标记低质量` 与 `20m 质量门，宁缺毋滥`
- 代码实际：`src/modules/Pim.Module.Mobile/Services/MobileLocationService.cs:17`  `MaxUsableAccuracyMeters = 50` 且 `>=50` 拒收；`MobileLocationQueryService.cs:8` `DefaultMaxAccuracyMeters = 50`；`MobileLocationAggregationService.cs:559` `<= MaxAccuracyMeters` 判定可用；客户端阈值一致为 50m
- 差异：文档承诺 20m，但代码实际以 50m 作为可用/拒绝与聚合边界，20m 的点在代码中被视为可用，不符合文档描述的收敛标准
