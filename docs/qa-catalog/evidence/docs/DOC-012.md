# DOC-012 | docs/ops-readonly-api.md | 一般 | 日志与 DB 截断阈值文档描述与旧实现残留的每分钟限流混淆
- 描述：运维文档在限流章节已更新为并发 2 与单次 5MB/10s，但旧设计稿与早期实现曾包含 30次/min 与 5MB/min，文档间存在交叉引用风险。
- 文档位置：ops-readonly-api.md:221-229 限流与截断章节 与 297-302 审计可观测章节
- 文档承诺：`并发 单 IP 并发 2 | 429 ... + Retry-After:5` 与 `单次 500行/5MB/10s 截断保持在 OpsLogsService/OpsDbService（206 Partial）`，并声明 `30次/min+5MB/min 已移除`
- 代码实际：`OpsRateLimiter.cs:9` `MaxConcurrent=2`，`OpsRateLimitMiddleware.cs:26` 按 `OpsIpHelper.GetClientIp` 计数并发；`OpsLogsService.cs:17-18` `MaxLimit=500 MaxBytes=5MB MaxDuration=10s` 与 `OpsDbService.cs` 同阈值；但 `specs/2026-08-21-ops-readonly-api-design.md:127` 仍列 `限流（30/min,5MB/min,10s）` 导致读者若同时参考两份文档会得到不同阈值
- 差异：正式文档已修正，但设计稿未同步，易造成按旧阈值压测得到预期不符的误判
