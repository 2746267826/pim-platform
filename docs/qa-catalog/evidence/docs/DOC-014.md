# DOC-014 | docs/operations/pc-facts-stage1-acceptance.md | 提示 | 原始数据保存描述与 KeyStats 日级兼容路径的保留说明易引起误读
- 描述：Stage1 验收在 roadmap gap closure 列标注日级兼容路径仍可用，但重点转向分钟采样，易让读者误以为日级为推荐来源。
- 文档位置：operations/pc-facts-stage1-acceptance.md:15-16 表格 Save KeyStats daily compatibility data 行
- 文档承诺：`| /api/v1/pc/keystats/upload keeps the existing daily compatibility path. | Daily upload remains available while Stage 1 validation focuses on minute samples...|`
- 代码实际：`PcTrackerModule.cs:58-67` 同时保留 `POST /keystats/upload`（日级）与 `POST /keystats/samples`（分钟级）；`PcActivityAggregationService.cs` 与 `PcTrackerQualityService.cs` 仅以分钟样本计算 delta 与质量，`docs/plan.md` 亦强调分钟 delta 为可靠来源
- 差异：文档未明确声明日级接口已 deprecate/仅兼容，读者可能继续依赖日级数据而得到不可靠的 delta，与代码以分钟样本为准的实际可靠性策略不一致
