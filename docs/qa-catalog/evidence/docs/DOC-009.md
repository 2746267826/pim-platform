# DOC-009 | docs/superpowers/specs/2026-08-21-ops-readonly-api-design.md | 一般 | 设计稿中 CIDR 白名单与限流旧阈值已在实现中移除但设计未同步
- 描述：运维只读设计稿仍描述 `PIM_OPS_ALLOWED_CIDRS` 与按分钟限流，但正式实现与使用文档已移除这些能力。
- 文档位置：specs/2026-08-21-ops-readonly-api-design.md:13 鉴权表格、42 配置段、59 变量段、127 验证清单
- 文档承诺：`可选 PIM_OPS_ALLOWED_CIDRS 白名单`；`限流 30/min,5MB/min,10s`；`OpsOptions.AllowedCidrs` 存在
- 代码实际：`src/Pim.Api/Infrastructure/Ops/OpsKeyValidator.cs` 仅校验 `PIM_OPS_KEY`，无 CIDR 参数；`OpsRateLimiter.cs:8-22` 仅单 IP 并发 2，`Retry-After:5`，无每分钟计数；`docs/ops-readonly-api.md:227` 明确 `30次/min+5MB/min 已移除`；`docker-compose.prod.yml` 与 `.env.prod.example` 无 `PIM_OPS_ALLOWED_CIDRS`
- 差异：设计稿承诺的 CIDR 白名单与分钟级限流在代码中不存在，设计与实现不一致
