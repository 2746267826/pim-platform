# src/Pim.Infrastructure/Data/Migrations/20260526144517_Stage5CalendarTaskLoop.Designer.cs

## 元信息
- 语言：C#（EF Core 自动生成 Designer 快照）
- 程序集或包：Pim.Infrastructure
- 职责：迁移 `20260526144517_Stage5CalendarTaskLoop` 的目标模型快照；`BuildTargetModel` 描述该迁移完成后 `PimDbContext` 的完整实体/表/关系模型。
- 主要依赖：`Microsoft.EntityFrameworkCore*`、`Npgsql.EntityFrameworkCore.PostgreSQL`、`PimDbContext` 及各实体类型名字符串
- 被谁使用：EF 迁移工具与 `dotnet ef`；与同名 `.cs` 迁移配对；不由应用运行时直接调用

## 函数级结构化伪代码

### Stage5CalendarTaskLoop（partial）
#### protected override void BuildTargetModel(ModelBuilder modelBuilder)
- 输入：`ModelBuilder`
- 输出：无；在 builder 上配置完整目标模型
- 副作用：填充注解、实体属性、键、索引、外键与导航
- 步骤：
  1. 注解：ProductVersion `8.0.11`；标识符最大长度 63；启用 Npgsql IdentityByDefault
  2. 配置平台实体：`AuditLogEntity`→`audit_logs`，`DaemonHeartbeatEntity`→`daemon_heartbeats`，`LoginAttemptEntity`→`login_attempts`，`OperationConfirmationEntity`→`operation_confirmations`，`RefreshTokenEntity`→`refresh_tokens`，`UserEntity`→`users`（Username/Email 唯一索引）
  3. 配置 Calendar：`CalendarEntity`/`EventEntity`/`OutlookConnectionEntity`/`PendingConfirmationEntity`/`SchedulingFeedbackEntity`/`TaskEntity` → 对应 `calendars`/`events`/`outlook_connections`/`pending_confirmations`/`scheduling_feedback`/`tasks`（本迁移配套 Up 为 tasks/events/calendars 增加软删操作字段、事件 ICS/时区/全天/例外日等列与索引）
  4. 配置 PcTracker：分类规则/分类/设置/建议、App 分类、AW bucket/event、Keystats 日/应用分解/按键计数/采样 → `pc_*` 表
  5. 配置 QuickNotes：`QuickNoteEntity`/`QuickNoteAttachmentEntity` → `quick_notes`/`quick_note_attachments`
  6. 配置关系：LoginAttempt/RefreshToken→User；Event/Task→Calendar；Task 自引用 Parent/SubTasks；Keystats 子表→Daily；Attachment→Note；导航集合注册
- 分支与异常：无运行时分支；`#pragma warning disable 612,618` 包裹过时 API
- 调用：`modelBuilder.Entity`、`HasKey`/`HasIndex`/`ToTable`/`HasOne`/`WithMany`/`Navigation`

## 近逐行中文伪代码

1. 文件头：`// <auto-generated />`；引入 EF/Migrations/Npgsql/Pim.Infrastructure.Data
2. `#nullable disable`；命名空间 `Pim.Infrastructure.Data.Migrations`
3. 特性：`[DbContext(typeof(PimDbContext))]`、`[Migration("20260526144517_Stage5CalendarTaskLoop")]`
4. partial 类 `Stage5CalendarTaskLoop`
5. 重写 `BuildTargetModel`：禁用 612/618 警告
6. 设置模型注解与 Npgsql Identity 默认
7. 依次 `modelBuilder.Entity("...Entity")` 声明属性类型/列名/长度/默认值，再 HasKey/HasIndex/ToTable
8. 实体集合（主配置块）：AuditLog、DaemonHeartbeat、LoginAttempt、OperationConfirmation、RefreshToken、User、Calendar、Event、OutlookConnection、PendingConfirmation、SchedulingFeedback、Task、ActivityCategoryRule、ActivityClassification、ActivityClassificationSettings、ActivityClassificationSuggestion、AppCategory、AwBucket、AwEvent、KeystatsAppBreakdown、KeystatsDaily、KeystatsKeyCount、KeystatsSample、QuickNoteAttachment、QuickNote
9. 表：audit_logs、daemon_heartbeats、login_attempts、operation_confirmations、refresh_tokens、users、calendars、events、outlook_connections、pending_confirmations、scheduling_feedback、tasks、pc_activity_*、pc_app_categories、pc_aw_*、pc_keystats_*、quick_note_attachments、quick_notes
10. 关系块：User FK（LoginAttempt、RefreshToken Cascade）、Calendar→Events/Tasks、Task 自引用、Keystats 级联、Attachment SetNull
11. Navigation 集合；恢复 pragma；方法结束

## 关系边
```json
{
  "nodes": [
    {
      "id": "src/Pim.Infrastructure/Data/Migrations/20260526144517_Stage5CalendarTaskLoop.Designer.cs",
      "label": "Stage5CalendarTaskLoop.Designer",
      "path": "src/Pim.Infrastructure/Data/Migrations/20260526144517_Stage5CalendarTaskLoop.Designer.cs",
      "doc": "docs/pseudocode/files/src/Pim.Infrastructure/Data/Migrations/20260526144517_Stage5CalendarTaskLoop.Designer.cs.md",
      "layer": "infrastructure",
      "kind": "other"
    }
  ],
  "edges": [
    { "from": "src/Pim.Infrastructure/Data/Migrations/20260526144517_Stage5CalendarTaskLoop.Designer.cs", "to": "src/Pim.Infrastructure/Data/PimDbContext.cs", "type": "depends_on" },
    { "from": "src/Pim.Infrastructure/Data/Migrations/20260526144517_Stage5CalendarTaskLoop.Designer.cs", "to": "src/Pim.Infrastructure/Data/Migrations/20260526144517_Stage5CalendarTaskLoop.cs", "type": "depends_on" },
    { "from": "src/Pim.Infrastructure/Data/Migrations/20260526144517_Stage5CalendarTaskLoop.Designer.cs", "to": "src/Pim.Infrastructure/Data/Entities/UserEntity.cs", "type": "depends_on" },
    { "from": "src/Pim.Infrastructure/Data/Migrations/20260526144517_Stage5CalendarTaskLoop.Designer.cs", "to": "Microsoft.EntityFrameworkCore", "type": "depends_on" }
  ]
}
```
