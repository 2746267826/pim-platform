# src/Pim.Infrastructure/Data/Migrations/20260527025542_AddAiGateway.cs

## 元信息
- 语言：C#
- 程序集或包：Pim.Infrastructure
- 职责：EF Core 迁移 `AddAiGateway`：创建 AI 提供商设置表与请求日志表及索引。
- 主要依赖：`Microsoft.EntityFrameworkCore.Migrations`、`MigrationBuilder`
- 被谁使用：EF 迁移管线 / `dotnet ef database update`；模型快照后续版本包含这些表

## 函数级结构化伪代码

### AddAiGateway
#### protected override void Up(MigrationBuilder migrationBuilder)
- 输入：迁移构建器
- 输出：无
- 副作用：创建表与索引
- 步骤：
  1. 创建 `ai_provider_settings`：id、provider(默认 litellm)、base_url、virtual_key_secret(bytea)、default_model、status(默认 disabled)、健康检查时间/错误、created/updated(now())
  2. 创建 `ai_request_logs`：请求上下文（user/module/purpose/source）、provider/model、correlation、状态与尝试次数、起止时间与 duration、请求/响应 jsonb 与文本、schema 字段、token/费用、hash、错误、metadata
  3. 索引：provider 唯一、status；logs 上 correlation/model/module/purpose/source 组合/started_at/status/user_id
- 分支与异常：无（DDL）
- 调用：`CreateTable`、`CreateIndex`

#### protected override void Down(MigrationBuilder migrationBuilder)
- 输入：迁移构建器
- 输出：无
- 副作用：删除两表
- 步骤：Drop `ai_provider_settings`、`ai_request_logs`
- 分支与异常：无
- 调用：`DropTable`

## 近逐行中文伪代码

1. 自动生成风格 partial 类 `AddAiGateway : Migration`
2. Up：建 `ai_provider_settings`（虚拟密钥密文、默认模型、状态）
3. Up：建 `ai_request_logs`（完整请求/响应/Schema 校验/用量字段，jsonb 默认 `[]`/`{}`）
4. Up：provider 唯一索引 + 多列查询索引
5. Down：两表 Drop

## 关系边
```json
{
  "nodes": [
    {
      "id": "src/Pim.Infrastructure/Data/Migrations/20260527025542_AddAiGateway.cs",
      "label": "AddAiGateway",
      "path": "src/Pim.Infrastructure/Data/Migrations/20260527025542_AddAiGateway.cs",
      "doc": "docs/pseudocode/files/src/Pim.Infrastructure/Data/Migrations/20260527025542_AddAiGateway.cs.md",
      "layer": "infrastructure",
      "kind": "other"
    }
  ],
  "edges": [
    { "from": "src/Pim.Infrastructure/Data/Migrations/20260527025542_AddAiGateway.cs", "to": "Microsoft.EntityFrameworkCore.Migrations", "type": "depends_on" },
    { "from": "src/Pim.Infrastructure/Data/Migrations/PimDbContextModelSnapshot.cs", "to": "src/Pim.Infrastructure/Data/Migrations/20260527025542_AddAiGateway.cs", "type": "depends_on" }
  ]
}
```
