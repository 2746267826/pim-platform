# src/Pim.Infrastructure/Data/Migrations/20260527025542_AddAiGateway.Designer.cs

## 元信息
- 语言：C#（EF Core 自动生成 Designer）
- 程序集或包：Pim.Infrastructure
- 职责：迁移 `AddAiGateway` 完成后的目标模型快照；相对基线新增 AI Provider/请求日志，并包含此前阶段已有的运营表、QuickNotes、PC 分类持久化等实体
- 主要依赖：EF Core Migrations/Infrastructure、Npgsql、`PimDbContext`
- 被谁使用：EF 工具链与 `AddAiGateway` partial；业务不直接调用

## 函数级结构化伪代码

### AddAiGateway（partial）
#### 特性
- 输入：无
- 输出：绑定 `PimDbContext` 与迁移 Id `20260527025542_AddAiGateway`
- 副作用：无
- 步骤：DbContext + Migration 注解
- 分支与异常：无
- 调用：无

#### protected override void BuildTargetModel(ModelBuilder modelBuilder)
- 输入：`ModelBuilder`
- 输出：无
- 副作用：构建含 AI 表在内的全量目标模型
- 步骤：
  1. ProductVersion 8.0.11；identity 列扩展
  2. **本迁移增量焦点**：`AiProviderSettingEntity`→`ai_provider_settings`（provider 默认 litellm、status 默认 disabled、base_url、virtual_key 等、created/updated now）
  3. `AiRequestLogEntity`→`ai_request_logs`（模块/用途/源对象、模型、correlation、attempt、JSON 载荷、token/费用精度 18,8、错误字段、多索引）
  4. 保留运营核心：`AuditLogEntity`、`DaemonHeartbeatEntity`、`OperationConfirmationEntity` 与 Auth 三表
  5. Calendar 六实体 + QuickNotes（`quick_notes`/`quick_note_attachments`）
  6. PcTracker 扩展：含 `ActivityClassificationEntity`、`ActivityClassificationSettingsEntity` 等
  7. 关系/Navigation 段：User FK、Calendar 父子任务、Keystats 子表、QuickNote 附件等
- 分支与异常：无
- 调用：`modelBuilder.Entity` 系列 Fluent API

### 相对基线新增/扩大的模型要点
- 新表：`ai_provider_settings`、`ai_request_logs`
- 相对 Baseline 已累积出现：`audit_logs`、`daemon_heartbeats`、`operation_confirmations`、`quick_notes`、`quick_note_attachments`、`pc_activity_classifications`、`pc_activity_classification_settings`
- 实体约 27；表约 27

## 近逐行中文伪代码

1. auto-generated 头与 using（同其它 Designer）
2. partial `AddAiGateway` + Migration 特性
3. `BuildTargetModel`：模型注解后首块即 `AiProviderSettingEntity` 全字段映射
4. 紧接 `AiRequestLogEntity`：attempt_number、token、estimated_cost、schema_*、request/response jsonb 默认值等
5. 其后按字母/模块顺序展开 Audit/Daemon/Login/Operation/Refresh/User 与各业务模块实体
6. 各实体统一：uuid 主键、snake_case 列名、HasIndex、ToTable
7. 文件尾部关系配置与 Navigation
8. pragma restore；结束

## 关系边
```json
{
  "nodes": [
    {
      "id": "src/Pim.Infrastructure/Data/Migrations/20260527025542_AddAiGateway.Designer.cs",
      "label": "AddAiGateway.Designer",
      "path": "src/Pim.Infrastructure/Data/Migrations/20260527025542_AddAiGateway.Designer.cs",
      "doc": "docs/pseudocode/files/src/Pim.Infrastructure/Data/Migrations/20260527025542_AddAiGateway.Designer.cs.md",
      "layer": "infrastructure",
      "kind": "other"
    }
  ],
  "edges": [
    { "from": "src/Pim.Infrastructure/Data/Migrations/20260527025542_AddAiGateway.Designer.cs", "to": "src/Pim.Infrastructure/Data/PimDbContext.cs", "type": "depends_on" },
    { "from": "src/Pim.Infrastructure/Data/Migrations/20260527025542_AddAiGateway.Designer.cs", "to": "src/Pim.Infrastructure/Data/Migrations/20260527025542_AddAiGateway.cs", "type": "depends_on" },
    { "from": "src/Pim.Infrastructure/Data/Migrations/20260527025542_AddAiGateway.Designer.cs", "to": "src/Pim.Infrastructure/Data/Entities", "type": "depends_on" },
    { "from": "src/Pim.Infrastructure/Data/Migrations/20260527025542_AddAiGateway.cs", "to": "ai_provider_settings", "type": "depends_on" },
    { "from": "src/Pim.Infrastructure/Data/Migrations/20260527025542_AddAiGateway.cs", "to": "ai_request_logs", "type": "depends_on" }
  ]
}
```
