# src/Pim.Infrastructure/Data/Migrations/20260524000000_BaselineExistingSchema.Designer.cs

## 元信息
- 语言：C#（EF Core 自动生成 Designer）
- 程序集或包：Pim.Infrastructure
- 职责：迁移 `BaselineExistingSchema` 的目标模型快照：在 ProductVersion 8.0.11 下描述基线库全部实体/表/关系（无运行时业务控制流）
- 主要依赖：`Microsoft.EntityFrameworkCore.*`、`Npgsql.EntityFrameworkCore.PostgreSQL`、`PimDbContext`
- 被谁使用：EF 工具链与 `BaselineExistingSchema` partial；不由应用业务直接调用

## 函数级结构化伪代码

### BaselineExistingSchema（partial）
#### 特性
- 输入：无
- 输出：标注本 partial 绑定 `PimDbContext` 与迁移 Id `20260524000000_BaselineExistingSchema`
- 副作用：无
- 步骤：`[DbContext(typeof(PimDbContext))]`、`[Migration("20260524000000_BaselineExistingSchema")]`
- 分支与异常：无
- 调用：无

#### protected override void BuildTargetModel(ModelBuilder modelBuilder)
- 输入：`ModelBuilder`
- 输出：无
- 副作用：在内存中构建该迁移完成后的完整模型（属性类型、列名 snake_case、索引、表名、导航/FK）
- 步骤：
  1. 关闭 612/618 警告；注解 ProductVersion=8.0.11、MaxIdentifierLength=63
  2. `UseIdentityByDefaultColumns`
  3. 配置核心 Auth 实体：`LoginAttemptEntity`→`login_attempts`；`RefreshTokenEntity`→`refresh_tokens`；`UserEntity`→`users`（Username/Email 唯一）
  4. 配置 Calendar：`CalendarEntity`→`calendars`；`EventEntity`→`events`（含 outlook_event_id）；`OutlookConnectionEntity`→`outlook_connections`；`PendingConfirmationEntity`→`pending_confirmations`；`SchedulingFeedbackEntity`→`scheduling_feedback`；`TaskEntity`→`tasks`
  5. 配置 PcTracker：活动规则/建议/应用分类、AW bucket/event、keystats 日/应用拆分/键计/采样 等表（`pc_*`）
  6. 关系段：LoginAttempt/RefreshToken→User；Event→Calendar；Task→Calendar 与 ParentTask 自引用；Keystats 子表→DailySnapshot；Navigation 补全
  7. 恢复 pragma
- 分支与异常：无（生成代码，无条件业务分支）
- 调用：`modelBuilder.Entity` / `Property` / `HasKey` / `HasIndex` / `ToTable` / `HasOne` / `WithMany` / `Navigation`

### 目标模型实体清单（本快照）
- 核心：LoginAttempt、RefreshToken、User
- Calendar：Calendar、Event、OutlookConnection、PendingConfirmation、SchedulingFeedback、Task
- PcTracker：ActivityCategoryRule、ActivityClassificationSuggestion、AppCategory、AwBucket、AwEvent、KeystatsAppBreakdown、KeystatsDaily、KeystatsKeyCount、KeystatsSample
- 表共 18：`users`/`login_attempts`/`refresh_tokens`/`calendars`/`events`/`outlook_connections`/`pending_confirmations`/`scheduling_feedback`/`tasks` 与 9 张 `pc_*`

## 近逐行中文伪代码

1. 文件头：`// <auto-generated />`；引入 System、EF Core Infrastructure/Migrations/ValueConversion、Npgsql Metadata、`Pim.Infrastructure.Data`
2. `#nullable disable`；命名空间 `Pim.Infrastructure.Data.Migrations`
3. partial 类 `BaselineExistingSchema` 挂 DbContext/Migration 特性
4. `BuildTargetModel` 开始：模型级注解与 PostgreSQL identity 扩展
5. 按实体块：每个实体先列 Property（uuid 主键、时间戳、varchar 长度、列名），再 HasKey/HasIndex/ToTable
6. `users`：password_hash、role、软删 deleted_at、用户名/邮箱唯一
7. Calendar/Event/Task 等带 user_id、outlook 字段与时间列
8. PcTracker 表多使用命名索引（如 `ux_pc_keystats_samples_device_minute`）
9. 文件后部二次 `modelBuilder.Entity` 仅配关系与 Navigation
10. pragma restore；方法与类结束

## 关系边
```json
{
  "nodes": [
    {
      "id": "src/Pim.Infrastructure/Data/Migrations/20260524000000_BaselineExistingSchema.Designer.cs",
      "label": "BaselineExistingSchema.Designer",
      "path": "src/Pim.Infrastructure/Data/Migrations/20260524000000_BaselineExistingSchema.Designer.cs",
      "doc": "docs/pseudocode/files/src/Pim.Infrastructure/Data/Migrations/20260524000000_BaselineExistingSchema.Designer.cs.md",
      "layer": "infrastructure",
      "kind": "other"
    }
  ],
  "edges": [
    { "from": "src/Pim.Infrastructure/Data/Migrations/20260524000000_BaselineExistingSchema.Designer.cs", "to": "src/Pim.Infrastructure/Data/PimDbContext.cs", "type": "depends_on" },
    { "from": "src/Pim.Infrastructure/Data/Migrations/20260524000000_BaselineExistingSchema.Designer.cs", "to": "src/Pim.Infrastructure/Data/Migrations/20260524000000_BaselineExistingSchema.cs", "type": "depends_on" },
    { "from": "src/Pim.Infrastructure/Data/Migrations/20260524000000_BaselineExistingSchema.Designer.cs", "to": "src/Pim.Infrastructure/Data/Entities", "type": "depends_on" },
    { "from": "src/Pim.Infrastructure/Data/Migrations/20260524000000_BaselineExistingSchema.Designer.cs", "to": "src/modules/Pim.Module.Calendar/Entities", "type": "depends_on" },
    { "from": "src/Pim.Infrastructure/Data/Migrations/20260524000000_BaselineExistingSchema.Designer.cs", "to": "src/modules/Pim.Module.PcTracker/Entities", "type": "depends_on" }
  ]
}
```
