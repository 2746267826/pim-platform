# DOC-002 | README.md | 一般 | 配置参考表仍保留已废弃的 SSH 环境变量
- 描述：README 配置参考列出 `PIM_SSH_PORT` 与 `PIM_SSH_AUTHORIZED_KEYS` 为必需/可选变量，但实际部署模板已移除这些变量。
- 文档位置：README.md:287-291 配置参考 表格
- 文档承诺：`| PIM_SSH_PORT | 宿主机 SSH 端口 | 否 |` 与 `| PIM_SSH_AUTHORIZED_KEYS | 容器 SSH 公钥 | 是 |`
- 代码实际：`.env.prod.example` 仅含 `PIM_OPS_KEY` 与 `PIM_OPS_RO_CONNECTION`（`grep PIM_SSH .env.prod.example` 无命中）；`docker-compose.prod.yml:40-41` 仅注入 `PIM_OPS_KEY`/`PIM_OPS_RO_CONNECTION`，无 SSH 变量
- 差异：文档要求用户填写的 SSH 变量在代码中不再被读取，实际生效的是 `PIM_OPS_KEY`，导致配置指引与可用变量不一致
