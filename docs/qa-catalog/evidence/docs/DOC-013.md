# DOC-013 | docs/operations/calendar-task-stage5-acceptance.md | 一般 | 事件分页未限流与文档隐含的 PagedResult 契约不一致
- 描述：Calendar 模块对 tasks 已做分页 clamp，但 events 未限流，验收文档未揭示该分裂。
- 文档位置：operations/calendar-task-stage5-acceptance.md:24-25 API Checks `GET /api/v1/calendar/tasks?search=...&page=1&pageSize=50` 与 `GET /api/v1/calendar/events`
- 文档承诺：验收列出 `page/pageSize` 分页能力，隐含符合 `module-development-guide.md` 的 `PagedResult` 与参数校验
- 代码实际：`CalendarService.cs:942` `GetTasksPagedAsync` 有 `Clamp(1,100)`，`CalendarModule.cs:329` `GetEventsPagedAsync` 无 clamp，`?page=-1&pageSize=10000` 可 `Take(10000)`；`evidence/calendar-pagesize.json` 已记录全量返回
- 差异：同模块内分页校验不一致，文档验收未覆盖 events 的边界校验，实际行为超出文档预期的分页约束
