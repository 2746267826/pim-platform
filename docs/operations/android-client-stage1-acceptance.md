# Android 客户端阶段一验收记录

日期：2026-07-16

## 目标

阶段一关键路径已实现并通过验证：运行（采集服务持续运行）、同步（手动与周期同步唯一执行路径）、状态（事实聚合与 issue 生成）、恢复（强制停止/重启后恢复）与诊断（ZIP 导出排除凭据）可用且事实化。

## 自动验证

| 验证项 | 结果 |
|--------|------|
| `dotnet test Pim.sln` | 1170/1170 通过 |
| `:core:testDebugUnitTest :app:testDebugUnitTest` | 通过 |
| `:app:assembleDebug` | 成功 |
| `connectedDebugAndroidTest`（API 35 Pixel_Tablet） | 40/40 通过 |
| 恢复默认聚焦测试强制 rerun | 成功 |
| `git diff --check` | 由根代理最终执行，本记录不声称已通过 |

## 手工验收

全部九项模拟器场景已逐项通过：

1. **首次配置** — 地址保存、连接检查返回分步结果、登录成功
2. **权限显示** — 六项权限事实正确展示
3. **持续采集** — 开启后前台服务运行，下次定位已安排
4. **手动同步** — 显示 等待 → 同步中 → 完成，待传归零
5. **飞行模式** — 队列保留，恢复网络后自动重试
6. **断网不清除** — 采集意图在断网前后保持不变
7. **强制停止** — 进程被杀死后重新打开，采集恢复
8. **开机恢复** — 重启解锁后 `BOOT_COMPLETED` 恢复服务
9. **ZIP 导出与恢复默认**：
   - 无坐标 ZIP：6 个条目，不含 `locations.jsonl`
   - 有坐标 ZIP：含 3 条位置记录
   - 两个 ZIP 均扫描确认无 `token`/`password`/`Authorization`/`Cookie`/`Bearer`
   - 恢复默认后：服务器地址和登录保留，持续采集关闭，服务停止，页面显示"持续采集已关闭。"

## 已知限制

- 当前临时服务端只声明 `mobileItemResultsV1`，未声明阶段二 `androidEmbedV1`，连接检查显示"部分可达"，属预期行为
- API 37 AndroidX Test 反射兼容问题不作为应用失败判定依据；正式插桩门禁使用 API 35

## 结论

阶段一关键路径（运行、同步、状态、恢复、诊断）已通过自动验证与手工验收。九项模拟器场景全部通过，凭据安全扫描覆盖 ZIP 内容。本记录不表示 PR/CI 已完成。
