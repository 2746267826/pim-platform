# src/Pim.Infrastructure/Data/Migrations/20260524170037_Stage0OperationsTables.Designer.cs

## 元信息
- 语言：C#（EF Core 自动生成 Designer）
- 程序集或包：Pim.Infrastructure
- 职责：为迁移 `20260524170037_Stage0OperationsTables` 提供 `BuildTargetModel` 目标模型快照（该迁移应用后的完整模型），含 Stage0 运维表与基线业务/PC 表共 21 张。
- 主要依赖：
  - `Microsoft.EntityFrameworkCore` / `Migrations` / `Infrastructure`
  - `Npgsql.EntityFrameworkCore.PostgreSQL.Metadata`
  - `PimDbContext`（`[DbContext]`）
- 被谁使用：EF 迁移基础设施与 `Stage0OperationsTables` partial 类；不直接由业务调用

## 函数级结构化伪代码

### Stage0OperationsTables（partial）
#### 特性与类声明
- 输入：无
- 输出：与迁移 `20260524170037_Stage0OperationsTables` 绑定的 partial 类
- 副作用：无运行时副作用（编译期元数据）
- 步骤：
  1. `[DbContext(typeof(PimDbContext))]`
  2. `[Migration("20260524170037_Stage0OperationsTables")]`
  3. `partial class Stage0OperationsTables`
- 分支与异常：无
- 调用：无

#### `protected override void BuildTargetModel(ModelBuilder modelBuilder)`
- 输入：`ModelBuilder`
- 输出：void（配置完整目标模型）
- 副作用：向 modelBuilder 注册实体、键、索引、表名、关系
- 步骤：
  1. 注解 `ProductVersion=8.0.11`、`Relational:MaxIdentifierLength=63`；`UseIdentityByDefaultColumns`。
  2. 逐实体 `modelBuilder.Entity(...)`：属性类型/列名/默认值/MaxLength → `HasKey` → `HasIndex` → `ToTable`。
  3. 核心/运维实体：`AuditLogEntity`→`audit_logs`；`DaemonHeartbeatEntity`→`daemon_heartbeats`；`LoginAttemptEntity`→`login_attempts`；`OperationConfirmationEntity`→`operation_confirmations`（索引 ExpiresAt/OperationType/RequestedByUserId/Status）；`RefreshTokenEntity`→`refresh_tokens`；`UserEntity`→`users`。
  4. Calendar：`calendars`/`events`/`outlook_connections`/`pending_confirmations`/`scheduling_feedback`/`tasks`。
  5. PcTracker：`pc_activity_category_rules`、`pc_activity_classification_suggestions`、`pc_app_categories`、`pc_aw_buckets`、`pc_aw_events`、`pc_keystats_*`。
  6. 关系段：LoginAttempt/RefreshToken→User；Event/Task→Calendar（及 Task 自引用 Parent）；Keystats 子表→Daily（Cascade）。
- 分支与异常：无业务分支（生成代码）
- 调用：Fluent API（Property/HasKey/HasIndex/ToTable/HasOne/WithMany）

## 近逐行中文伪代码

1. 文件头 `// <auto-generated />`；引入 System、EF Core、Migrations、Npgsql、`Pim.Infrastructure.Data`。
2. `#nullable disable`；命名空间 `Pim.Infrastructure.Data.Migrations`。
3. DbContext/Migration 特性；partial `Stage0OperationsTables`。
4. 重写 `BuildTargetModel`：禁用 612/618 警告。
5. 设置 ProductVersion 与标识符长度；Npgsql 默认 Identity。
6. 为 21 个实体块配置列映射（uuid/timestamptz/jsonb/varchar/text 等）与表名。
7. **Stage0 增量相关重点**：`operation_confirmations`、`audit_logs`、`daemon_heartbeats` 进入模型（相对 Baseline）。
8. 后半段 `HasOne`/`WithMany`/`HasForeignKey`/`OnDelete` 配置导航与级联。
9. 恢复 pragma；方法结束。
10. 本文件不包含 `Up`/`Down` DDL（在同名非 Designer 迁移类中）。

## 关系边
```json
{
  "nodes": [
    {
      "id": "src/Pim.Infrastructure/Data/Migrations/20260524170037_Stage0OperationsTables.Designer.cs",
      "label": "Stage0OperationsTables.Designer",
      "path": "src/Pim.Infrastructure/Data/Migrations/20260524170037_Stage0OperationsTables.Designer.cs",
      "doc": "docs/pseudocode/files/src/Pim.Infrastructure/Data/Migrations/20260524170037_Stage0OperationsTables.Designer.cs.md",
      "layer": "infrastructure",
      "kind": "other"
    }
  ],
  "edges": [
    { "from": "src/Pim.Infrastructure/Data/Migrations/20260524170037_Stage0OperationsTables.Designer.cs", "to": "src/Pim.Infrastructure/Data/PimDbContext.cs", "type": "depends_on" },
    { "from": "src/Pim.Infrastructure/Data/Migrations/20260524170037_Stage0OperationsTables.cs", "to": "src/Pim.Infrastructure/Data/Migrations/20260524170037_Stage0OperationsTables.Designer.cs", "type": "depends_on" }
  ]
}
```
