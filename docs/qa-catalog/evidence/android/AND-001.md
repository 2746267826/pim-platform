# AND-001 | 测试 | 严重 | connectedAndroidTest 全量 62 例末尾进程崩溃（Process crashed）

- 描述：`./gradlew connectedAndroidTest`（全模块）因 `:core:connectedDebugAndroidTest` 缺少 `androidx.test.runner.AndroidJUnitRunner` 导致 `ClassNotFoundException` 直接 FAILED；单跑 `:app:connectedDebugAndroidTest` 时 62 例中 50+ 通过、1 例 skipped，但 instrumentation 末尾 `Process crashed. 08-24 02:43:38.310 Zygote Process 6983 exited due to signal 9 (Killed)`，测试框架报 `INSTRUMENTATION_RESULT: shortMsg=Process crashed.`。
- 复现：`cd /workspace/pim-platform/src/client-android && ./gradlew connectedAndroidTest 2>&1 | tail` 复现 `:core` 失败；`./gradlew :app:connectedDebugAndroidTest 2>&1 | tail` 复现 62 例跑到末尾崩溃；单例 `./gradlew :app:connectedDebugAndroidTest -Pandroid.testInstrumentationRunnerArguments.class=com.pim.app.ui.location.LocationScreenTest#cancelButtonTriggersOnCancelCallback` 则 PASS。
- 预期：`connectedAndroidTest` 全量通过，`core` 不应参与 instrumentation 或应声明 runner；`:app` 62 例全部 PASS 且无进程崩溃。
- 实际：`:core` 报 `Unable to instantiate instrumentation ComponentInfo{com.pim.core.test/androidx.test.runner.AndroidJUnitRunner}: ClassNotFoundException`（证据见 logcat 5361）；`:app` 报 `Test run failed to complete. Instrumentation run failed due to Process crashed.`，5 tests 执行后崩溃（第一次全量）或 50 tests 后崩溃（第二次全量，中途被中断），最终 `BUILD FAILED`。
- 证据：`core/build/reports/androidTests/connected/debug/index.html`、`app/build/reports/androidTests/connected/debug/index.html`（5 tests, 1 failures, 1 skipped）、`app/build/outputs/androidTest-results/connected/debug/test_avd_36(AVD) - 16/testlog/test-results.log`、`app/build/outputs/androidTest-results/connected/debug/test_avd_36(AVD) - 16/logcat-com.pim.app.ui.location.LocationScreenTest-cancelButtonTriggersOnCancelCallback.txt`、`adb logcat -d | grep AndroidRuntime | grep 6983`（`signal 9`）、`/tmp/emu_latest.log`（Boot completed in 37287 ms）
