# src/Pim.Infrastructure/Data/Migrations/20260527042125_AddFilesModule.Designer.cs

## 元信息
- 语言：C#（EF Core 自动生成 Designer）
- 程序集或包：Pim.Infrastructure
- 职责：迁移 `20260527042125_AddFilesModule` 的目标模型快照；在运维/日历/PC/速记模型之上纳入 Files 模块 7 表，共 32 张表。
- 主要依赖：EF Core Migrations、Npgsql 元数据、`PimDbContext`
- 被谁使用：EF 迁移工具链与 `AddFilesModule` partial；业务不直接引用

## 函数级结构化伪代码

### AddFilesModule（partial）
#### 特性与类声明
- 输入：无
- 输出：绑定 MigrationId `20260527042125_AddFilesModule`
- 副作用：无
- 步骤：`[DbContext(typeof(PimDbContext))]` + `[Migration(...)]` + partial class
- 分支与异常：无
- 调用：无

#### `protected override void BuildTargetModel(ModelBuilder modelBuilder)`
- 输入：ModelBuilder
- 输出：void
- 副作用：完整模型配置
- 步骤：
  1. 全局注解 ProductVersion 8.0.11、MaxIdentifierLength 63、Identity 默认列。
  2. 继承既有：运维（audit/daemon/login/operation_confirmations/refresh/users）、Calendar 六表、PcTracker（含 classifications/settings）、QuickNotes（`quick_notes`/`quick_note_attachments`）。
  3. **Files 新增实体→表**：
     - `FileAiResultEntity`→`file_ai_results`
     - `FileChunkEntity`→`file_chunks`
     - `FileIndexJobEntity`→`file_index_jobs`
     - `FileItemEntity`→`file_items`（唯一索引 ProviderId+ExternalFileId；路径/父级/删除索引）
     - `FileProviderEntity`→`file_providers`
     - `FileSuggestionEntity`→`file_suggestions`
     - `FileVersionEntity`→`file_versions`
  4. 关系：Files 子实体级联到 FileItem/Version；FileItem→Provider Cascade；CurrentVersion Restrict；QuickNoteAttachment→QuickNote SetNull；保留 Calendar/PC 既有 FK。
- 分支与异常：无
- 调用：Fluent API

## 近逐行中文伪代码

1. auto-generated 头与标准 using；`#nullable disable`。
2. Migration `20260527042125_AddFilesModule`；partial `AddFilesModule`。
3. `BuildTargetModel`：全局注解后按实体字母/模块顺序配置。
4. 前半：AuditLog…User…Calendar…（与 Stage0/QuickNotes 路径一致）。
5. 中段：七个 Files 实体完整属性 + 键索引 + ToTable。
6. 后段：PcTracker + QuickNotes 实体。
7. 关系配置段：User FK、Calendar/Task、**Files 导航图**、Keystats、QuickNote 附件。
8. 关闭警告 pragma；结束。本文件不写 DDL Up/Down。

## 关系边
```json
{
  "nodes": [
    {
      "id": "src/Pim.Infrastructure/Data/Migrations/20260527042125_AddFilesModule.Designer.cs",
      "label": "AddFilesModule.Designer",
      "path": "src/Pim.Infrastructure/Data/Migrations/20260527042125_AddFilesModule.Designer.cs",
      "doc": "docs/pseudocode/files/src/Pim.Infrastructure/Data/Migrations/20260527042125_AddFilesModule.Designer.cs.md",
      "layer": "infrastructure",
      "kind": "other"
    }
  ],
  "edges": [
    { "from": "src/Pim.Infrastructure/Data/Migrations/20260527042125_AddFilesModule.Designer.cs", "to": "src/Pim.Infrastructure/Data/PimDbContext.cs", "type": "depends_on" },
    { "from": "src/Pim.Infrastructure/Data/Migrations/20260527042125_AddFilesModule.cs", "to": "src/Pim.Infrastructure/Data/Migrations/20260527042125_AddFilesModule.Designer.cs", "type": "depends_on" }
  ]
}
```
