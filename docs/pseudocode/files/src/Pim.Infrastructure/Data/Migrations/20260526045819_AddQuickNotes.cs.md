# src/Pim.Infrastructure/Data/Migrations/20260526045819_AddQuickNotes.cs

## 元信息
- 语言：C#
- 程序集或包：Pim.Infrastructure
- 职责：EF Core 迁移 `AddQuickNotes`：创建速记表 `quick_notes` 与附件表 `quick_note_attachments` 及索引。
- 主要依赖：`Microsoft.EntityFrameworkCore.Migrations`（`Migration`、`MigrationBuilder`）
- 被谁使用：EF 迁移管道 / `dotnet ef database update`；与 QuickNotes 模块实体对应

## 函数级结构化伪代码

### AddQuickNotes
#### `void Up(MigrationBuilder migrationBuilder)`
- 输入：`migrationBuilder`
- 输出：无
- 副作用：DDL 创建两表与多索引
- 步骤：
  1. 创建 `quick_notes`：`id` PK；`user_id`；`content_markdown`（默认 `""`）；`status`（默认 `inbox`）；`source`（默认 `web-page`）；`metadata_json` jsonb（默认 `{}`）；`created_at`/`updated_at`（默认 `now()`）；可空 `archived_at`/`deleted_at`
  2. 创建 `quick_note_attachments`：`id` PK；可空 `quick_note_id` FK→`quick_notes.id`（`SetNull`）；`user_id`；存储字段（`storage_provider` 默认 `minio`、`object_key`、`file_name`、`content_type`、`size_bytes`、`content_hash`、`metadata_json`、时间戳）
  3. 索引：附件按 `quick_note_id`、`(user_id, created_at)`、`(user_id, deleted_at)`；笔记按 `(user_id, created_at)`、`(user_id, status, updated_at)`
- 分支与异常：迁移失败由 EF 抛出
- 调用：`CreateTable`、`CreateIndex`、`PrimaryKey`、`ForeignKey`

#### `void Down(MigrationBuilder migrationBuilder)`
- 输入：`migrationBuilder`
- 输出：无
- 副作用：按依赖顺序删表
- 步骤：
  1. `DropTable quick_note_attachments`
  2. `DropTable quick_notes`
- 分支与异常：无
- 调用：`DropTable`

## 近逐行中文伪代码

1. 引入 `System` 与 EF Migrations
2. `#nullable disable`；命名空间 `Pim.Infrastructure.Data.Migrations`
3. 分部类 `AddQuickNotes : Migration`
4. `Up`：创建 `quick_notes` 表及主键 `PK_quick_notes`
5. 创建 `quick_note_attachments` 表，FK `quick_note_id` → `quick_notes.id` 删除时 SetNull
6. 为附件建 `quick_note_id`、用户+时间、用户+删除时间索引
7. 为笔记建用户+创建时间、用户+状态+更新时间索引
8. `Down`：先删附件表再删笔记表
9. （文件结束）

## 关系边
```json
{
  "nodes": [
    {
      "id": "src/Pim.Infrastructure/Data/Migrations/20260526045819_AddQuickNotes.cs",
      "label": "AddQuickNotes",
      "path": "src/Pim.Infrastructure/Data/Migrations/20260526045819_AddQuickNotes.cs",
      "doc": "docs/pseudocode/files/src/Pim.Infrastructure/Data/Migrations/20260526045819_AddQuickNotes.cs.md",
      "layer": "infrastructure",
      "kind": "other"
    }
  ],
  "edges": [
    { "from": "src/Pim.Infrastructure/Data/Migrations/20260526045819_AddQuickNotes.cs", "to": "Microsoft.EntityFrameworkCore.Migrations", "type": "depends_on" },
    { "from": "src/Pim.Infrastructure/Data/Migrations/20260526045819_AddQuickNotes.cs", "to": "src/modules/Pim.Module.QuickNotes", "type": "depends_on" }
  ]
}
```
