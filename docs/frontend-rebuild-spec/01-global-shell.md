# 01 全局框架（Global Shell）

## 1. 应用形态

- **单页应用（SPA）**：React 19 + TypeScript + Vite；路由 react-router v7（BrowserRouter）；数据层 TanStack Query v5（唯一缓存层）；UI 语言 zh-CN。
- **部署形态**：构建产物输出到后端 API 的静态目录，由 API 同源伺服（生产环境 SPA 与 API 同源；开发环境由 Vite 开发服务器代理 `/api` → `http://localhost:5858`）。前端不需要独立部署的后端。
- **依赖的关键能力型组件库**（重建时需要等价能力，不限定同款）：完整日历网格（月/周视图、拖选时间段、外部拖入）、图表引擎（折线/柱状/环形/漏斗/仪表盘/自定义热力矩阵）、地图引擎（瓦片底图 + 折线轨迹 + 圆形覆盖物 + 自定义标记）、富文本/Markdown 编辑器、上传队列、树控件、图片灯箱、Toast 通知。

## 2. 顶层布局结构

```
┌──────────────────────────────────────────────┐
│ 侧边栏（桌面常驻；移动端折叠为汉堡+抽屉）        │
│  ① 一级导航 16 项（见 §3）                     │
│  ② 日历本管理器（CRUD+显隐）                    │
│  ③ 任务本只读列表（点击跳任务页）               │
│  ④ 系统状态指示点（链接到 /status）             │
│  ⑤ 用户名 + 版本 + 登出                        │
├──────────────────────────────────────────────┤
│ 主内容区（每页独立 ErrorBoundary + 加载占位）   │
│ （日历页右侧额外停靠"收件箱侧板"，见 §5）        │
├──────────────────────────────────────────────┤
│ 页脚：本地版本 / API 版本 / "有可用更新"标记     │
└──────────────────────────────────────────────┘
全局浮层：快捷记录 FAB（除 /quick-notes 外所有页）
全局 Toast 通知（API 错误统一上浮、去重）
```

- **移动端适配**：侧边栏折叠为顶部汉堡按钮 + 背景遮罩抽屉；每页有移动版页头（标题+返回/操作）；宽表格在窄屏降级为卡片列表；三栏文件页在窄屏改为"树抽屉 + 全屏预览"；编辑面板在窄屏变为右侧抽屉。

## 3. 一级导航（16 项，每项带 1 字符徽标位）

| # | 导航项 | 路由 |
|---|---|---|
| 1 | 今日 | `/today` |
| 2 | 日历 | `/calendar` |
| 3 | 工作台 | `/workbench` |
| 4 | 确认 | `/confirmations` |
| 5 | 数据中心 | `/data-center` |
| 6 | 提醒 | `/reminders` |
| 7 | 报告 | `/reports` |
| 8 | 习惯 | `/habits` |
| 9 | 快速记录 | `/quick-notes` |
| 10 | 文件 | `/files` |
| 11 | 任务 | `/tasks` |
| 12 | 电脑记录 | `/pc-tracker` |
| 13 | 浏览器使用 | `/pc-tracker/browser` |
| 14 | 手机记录 | `/mobile-records` |
| 15 | 历史位置 | `/location-history` |
| 16 | 设置 | `/settings` |

- **侧边栏内嵌的日历本管理器**（形式：可折叠分组列表）：列出全部日历本，支持新建（内联输入）、双击内联重命名、显隐开关（控制日历图层过滤）、删除（先调删除影响预览接口 + 二次确认对话框，需输入确认）；点击某本跳转 `/calendar?calendarId=…`。
- **任务本只读列表**（形式：分组列表）：点击跳 `/tasks?taskBookId=…`，带任务计数。
- **系统状态指示点**（形式：状态圆点+链接）：轮询系统状态摘要接口，异常时呈告警态并链接 `/status`。
- 设置入口下的二级页面（数据可信度、同步、AI、MCP、日程数据管理、回收站、PC 明细查询、用户管理）不占一级导航，从 `/settings` 枢纽页的链接卡片进入。

## 4. 完整路由表

| 路由 | 页面 | 职责 |
|---|---|---|
| `/login` | 登录/注册 | 单卡片表单，登录/注册双模式切换（注册多一个邮箱字段）；成功后回跳原 URL |
| `/` | 重定向 → `/today` | — |
| `/today` | 今日 | 服务器驱动的分区仪表盘（详见 02） |
| `/calendar` | 日历 | 时间轴/月视图日历，`?view=`、`?calendarId=` |
| `/workbench` | 工作台 | 排程工作台/运营驾驶舱 |
| `/sync` | 重定向 → `/settings/sync` | 旧别名 |
| `/data-center` | 数据中心 | 跨对象治理/审计/恢复 |
| `/confirmations` | 确认中心 | 待确认操作处理 |
| `/reminders` | 提醒 | 提醒队列/规则/发送历史 |
| `/reports` | 报告 | 报告生成与浏览 |
| `/habits` | 习惯 | 习惯规则中心 |
| `/exhibition` | 展览馆 | 图表组件陈列馆（内部画廊页） |
| `/audit/:objectType/:objectId` | 审计时间线 | 单对象版本时间线 |
| `/endpoint-shell` | 端点外壳 | 设备端点心跳/采集质量调试页 |
| `/quick-notes` | 快速记录 | 闪念瀑布流看板 |
| `/files` | 文件 | OneDrive 三栏文件浏览器 |
| `/timeline`、`/week` | 重定向 → `/calendar?view=timeline` | 旧别名 |
| `/month` | 重定向 → `/calendar?view=month` | 旧别名 |
| `/tasks` | 任务 | 任务列表（过滤/层级/分段），`?taskBookId=` |
| `/pc-tracker` | 电脑记录 | PC 活动分析仪表盘 |
| `/pc-tracker/browser` | 浏览器使用 | 浏览器域名使用分析 |
| `/mobile-records` | 手机记录 | 手机使用分析（`?view=liveness` 设备存活子视图） |
| `/location-history` | 历史位置 | GPS 历史轨迹仪表盘 |
| `/devices` | 设备管理 | 移动设备管理（合并/改名/删除/导出） |
| `/devices/:deviceId` | 设备详情 | 单设备详情 |
| `/status` | 状态 | 系统/组件状态页 |
| `/settings` | 设置枢纽 | 链接卡片列表 |
| `/settings/data-reliability` | 数据可信度 | 13 规则数据体检 |
| `/settings/sync` | 同步设置 | Microsoft Outlook 同步 |
| `/settings/ai` | AI 设置 | LiteLLM 状态/用量/请求日志 |
| `/settings/mcp` | MCP 设置 | MCP 客户端/令牌/权限管理 |
| `/settings/calendar-data` | 日程数据管理 | 日程事件批量管理（ICS 导入导出/删除） |
| `/settings/recycle-bin` | 回收站 | 日历域回收站+恢复预览 |
| `/settings/pc-data` | PC 明细查询 | PC 原始记录明细查询 |
| `/settings/users` | 用户管理 | 管理员用户/角色管理（仅管理员可见） |
| `/app-knowledge-base` | 应用知识库 | 应用/域名知识库 |
| `/app-knowledge-base/categories` | 分类树 | 分类树编辑器 |
| `/pc-categories` | 重定向 → 分类树 | 旧别名 |
| `/pc-classification` | 重定向 → 应用知识库 | 旧别名 |
| `/embed/android/today` | Android 今日内嵌 | 无外壳内嵌页（Android 壳内 WebView） |
| `/embed/android/tracks` | Android 轨迹内嵌 | 无外壳内嵌页（历史位置页的紧凑变体） |
| `*` | 404 页 | 未匹配路由 |

- **免认证路由**：`/login`、`/embed/android/today`、`/embed/android/tracks`；其余全部被路由守卫保护，未登录重定向到 `/login`。
- Android 内嵌页不渲染侧边栏等外壳，仅渲染数据模块，并通过原生桥向壳上报页面数据状态。

## 5. 全局浮层模块

| 模块 | 出现范围 | 形式 | 调用接口 | 处理 |
|---|---|---|---|---|
| 快捷记录 FAB | 除 `/quick-notes` 外所有页 | 圆形悬浮按钮 → 弹出菜单（写闪念 / 建任务→任务页 / 排日程→日历页）；"写闪念"打开可拖拽的闪念编辑弹窗 | 闪念域接口（见 02 §quick-notes） | 弹窗位置记忆在 localStorage；编辑器支持粘贴/上传图片附件 |
| 收件箱侧板 | 仅 `/calendar` | 右侧停靠任务卡片列表：未排期/收件箱任务，优先级点+截止日；顶部"新建"菜单（任务/日程编辑弹窗）、"一键重排"按钮 | GET /api/v1/calendar/tasks（无参数，客户端过滤收件箱/未排期）、POST /api/v1/calendar/tasks/{id}/plan | 列表项为**拖拽源**：拖到日历时间槽触发排期（自动按时长估算）；标准轮询 |
| 全局 Toast | 所有页 | 轻提示堆栈 | — | 查询/变更失败统一上浮；5xx 在后台轮询或已有缓存数据时静音；按 key 去重 |

## 6. 认证流（形式级）

- **登录/注册页**：单卡片表单；登录（用户名+密码）/注册（用户名+邮箱+密码+显示名）双模式；内联错误提示；成功后存令牌并回跳。
- **令牌机制**：访问令牌 + 刷新令牌存 localStorage（内嵌页例外：由原生桥注入、不落盘）；所有请求自动带 `Authorization: Bearer`；任意请求遇到首个 401 时自动调刷新接口（并发请求共享同一次刷新），失败则清令牌回登录页。详见 [04-api-conventions.md](04-api-conventions.md)。
- **登出**：纯客户端清令牌（无服务端调用），回登录页。

## 7. 错误/空/加载态（形式类别清单）

| 类别 | 形式 |
|---|---|
| 页面级错误 | 错误边界：错误页（提示 + 重试 + 回首页）；404 页 |
| 模块级错误 | 卡片级错误边界（模块内提示，不影响整页） |
| 全局 API 错误 | Toast（去重、5xx 静音规则） |
| 加载中 | 骨架占位块 / 加载指示；页面级 Suspense |
| 空状态 | 统一空状态组件（图标位+一句话+可选操作）；占位框式空提示 |
| 操作结果 | 操作结果横幅（成功/失败+详情链接）；内联告警横幅 |
| 破坏性操作确认 | 确认对话框：列出受影响数量+样例行；日历删除等高危操作要求输入确认；确认中心为"两步武装确认" |

## 8. 共享 UI 原语（形式级清单，重建需提供等价物）

| 原语 | 形式 |
|---|---|
| 页头 | 标题 + 副标题 + 右侧操作区（含移动版变体） |
| 分段控件 | 胶囊型单选切换（视图/维度/标签页切换用） |
| 状态徽标 | 状态语义的短标签（健康/告警/离线等） |
| 指标卡 | 单指标数值+说明的卡片 |
| 编辑抽屉 | 右侧滑出、带焦点圈定的表单面板 |
| 确认对话框 | 见 §7 破坏性操作确认 |
| 空状态 | 见 §7 |
| 操作结果横幅 | 见 §7 |
| 弹窗/对话框族 | 各页面详列（任务编辑、日程编辑、闪念编辑、合并确认等） |
